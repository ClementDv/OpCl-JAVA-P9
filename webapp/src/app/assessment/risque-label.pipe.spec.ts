import { RisqueLabelPipe } from './risque-label.pipe';

describe('RisqueLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new RisqueLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
